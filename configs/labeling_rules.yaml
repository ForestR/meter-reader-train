# Rules applied during and after auto-labeling
reading:
  normal_length_min: 5   # inclusive
  normal_length_max: 6   # inclusive
  # readings outside [min, max] go to atypical/

sort:
  pending_review_dir: pending_review  # subdir for low-conf skipped images
  atypical_dir: atypical              # subdir for out-of-range reading length
